#include <stdio.h>
/**
 * Main(void) - Entry point
 */
int main(void)
	/**
	 * main - Entry point
	 *
	 * Return: Always o (Success)
	 */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
