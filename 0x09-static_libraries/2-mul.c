#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: number to be multiplied
 * @b: number to be multiplied
 *
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
