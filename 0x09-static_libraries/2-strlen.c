#include <string.h>
#include "main.h"
/**
 * _strlen - checks for the length of a string
 * @s: string parameter
 *
 * Return: 0 success
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
